# Superseded Machine Configuration Copy

This migration-local copy has been replaced by the repository root file:

```text
MACHINE_CONFIGURATION.md
```

Use the root file as the only current machine-configuration source of truth.
Keeping full copies under migration folders caused stale interpreter and
sequence-status notes to compete with the verified root handoff.
